let point={
    x:10,
    y:20,
    z:50
}

// '.' dot notation

console.log(point.x)
console.log(point.y+point.z)


// [] array notation use with string .used for unknown data

console.log(point['z'])
console.log(point['x']+point['z'])
